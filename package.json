{
  "name": "syncforge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --recursive run --parallel dev",
    "build": "pnpm --recursive run build",
    "test": "pnpm --recursive test",
    "lint": "pnpm --recursive run lint",
    "typecheck": "pnpm --recursive run typecheck"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
